# 项目背景

这是一个面向公众号编辑的工具，将输入的 Markdown 文字渲染成指定模板的公众号文章，从而复制到公众号进行发布。

## 项目结构

- `web/src/core`：Markdown 渲染和主题定义。
- `web/src/clipboard`：微信公众号富文本复制。
- `web/src/app`：React 界面和交互逻辑。
- `electron`：桌面端主进程、预加载脚本和 IPC。

## 工作规范

- 新增的项目源码注释使用中文，变量和函数使用英文。
- 修改前先说明涉及的文件和改动方案。用户明确要求“修改”“修复”或“实现”时，视为已经确认；如果影响范围明显扩大，需要重新确认。
- 只修改完成当前任务所必需的实现、类型和已有测试文件，不主动重构无关文件。

## 禁止项

- 不要删除任何文件，除非用户明确要求。
- 不要覆盖、还原或提交用户已有的无关修改。
- 未经确认，不执行依赖安装或升级命令，不切换包管理器，不修改 `package-lock.json`。

## 测试与验证

- 不主动新增测试，除非用户明确要求；行为发生变化时，可以更新已有测试。
- 修改完成后运行相关的现有检查，默认运行 `npm run check`；如果环境限制导致无法运行，需要说明原因。

## Commit 约束

- 只有用户明确要求时才创建提交。
- 提交信息使用 `<type>(<scope>): <message>` 格式；`scope` 可选，省略时使用 `<type>: <message>`。
- `type` 只使用：`feat`、`fix`、`docs`、`chore`。
- `scope` 用来标明影响范围，比如 `markdown`、`app`、`electron`、`readme`。
- `message` 用英文，简短描述本次变更，不以句号结尾。
- 提交前运行 `git status --short`、`git diff --cached --check` 和 `git diff --cached`，只提交本次任务相关文件。
